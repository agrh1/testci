# -----------------------------
# testCI - STAGING environment
# -----------------------------

# Обязательно: окружение
ENVIRONMENT=staging

# Включает строгий режим readiness (/ready вернёт 503, если не хватает обязательных настроек)
STRICT_READINESS=1

# -----------------------------
# Telegram bot
# -----------------------------
# Токен Telegram-бота (секрет)
TELEGRAM_BOT_TOKEN=__REPLACE_ME__

# Базовый URL web-сервиса внутри docker-compose сети.
# ВАЖНО: должен совпадать с именем сервиса в docker-compose (например web-staging)
WEB_BASE_URL=http://web-staging:8000

# -----------------------------
# ServiceDesk integration (задел под polling/уведомления)
# -----------------------------
# URL API сервис-деска (секрет/внутренний URL)
SERVICEDESK_BASE_URL=https://example.invalid/api
# Токен API сервис-деска (секрет)
SERVICEDESK_API_TOKEN=__REPLACE_ME__
SERVICEDESK_LOGIN=""
SERVICEDESK_PASSWORD=""
# (на будущее) чат/канал для уведомлений
ALERT_CHAT_ID=""
MIN_NOTIFY_INTERVAL_S=60
MAX_ITEMS_IN_MESSAGE=10